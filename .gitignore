*.log
*.zip
.DS_Store
.env
.env.backup
.env.local
.env.production
.env.railway
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/public/customer_product_returns/
/public/customer_supply_returns/
/public/customer_returns/
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
public (2).zip
public (3).zip
public.zip
resources (2).zip
resources (3).zip
resources (4).zip
resources (5).zip
resources (6).zip
resources (7).zip
resources.zip
routes (2).zip
routes (3).zip
routes.zip
vendor.zip
routes.zip
vendor.zip

/cdocs
/docs/sop-*.md

/storage/backups/

pegasuso_pegasus.sql

# Raw .sql dumps dropped at the repo root as snapshot:import-sql input (e.g.
# "Database baru.sql"). Never meant to be committed themselves — see
# database/seeders/README.md. The generated snapshot under
# database/seeders/{data,snapshots/<name>}/*.json is what gets committed.
/*.sql

/database/seeders/snapshots/prod_21082026
